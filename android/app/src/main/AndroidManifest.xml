<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Networking for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="${applicationName}"
        android:label="CinePulse"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false">
        <!-- ↑ Keep cleartext OFF in release. See debug manifest below to allow HTTP in dev. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme shown while Flutter initializes -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Optional: App Links (only works for REAL paths, not hash fragments) -->
            <!-- If you serve links like https://cinepusle.netlify.app/s/<id> you can
                 enable verified app links below. Adjust host to your actual domain. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Pick the correct host. Remove the one you don't use. -->
                <data android:scheme="https" android:host="cinepusle.netlify.app" android:pathPrefix="/s" />
                <data android:scheme="https" android:host="cinepulse.netlify.app" android:pathPrefix="/s" />
            </intent-filter>
            <!-- NOTE: Android will NOT receive URL fragments like #/s/<id>.
                 If your web app uses hash routing, switch to real paths for app links. -->
        </activity>

        <!-- Used by Flutter tooling -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Package visibility for Process Text (used by Flutter engine on some devices) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
